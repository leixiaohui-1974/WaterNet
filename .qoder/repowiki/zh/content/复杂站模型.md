# 复杂站模型

<cite>
**本文档引用文件**  
- [complex_station_model.py](file://waternet/models/complex_station_model.py)
- [configurable_gate_model.py](file://waternet/models/configurable_gate_model.py)
- [reservoir_model.py](file://waternet/models/reservoir_model.py)
- [boundary_conditions.py](file://waternet/models/boundary_conditions.py)
- [reservoir_gate_channel_system.yaml](file://configs/reservoir_gate_channel_system.yaml)
- [complex_hub_network.yaml](file://examples/configs/complex_hub_network.yaml)
- [config_driven_reservoir_gate_system.py](file://examples/config_driven_reservoir_gate_system.py)
</cite>

## 目录
1. [系统架构](#系统架构)
2. [设备层建模](#设备层建模)
3. [控制策略集成](#控制策略集成)
4. [水力模型耦合方式](#水力模型耦合方式)
5. [梯级水库调度应用案例](#梯级水库调度应用案例)

## 系统架构

复杂站模型采用层次化设计，将水力枢纽系统分解为设备层、控制层和系统层三个层级，实现从局部到整体的统一建模。该模型通过`ComplexStationModel`类实现，能够集成多种水力机械设备（如闸门、泵站、阀门等），并统一处理其流量特性、控制逻辑和系统级水量平衡。

模型通过定义上下游连接节点（`upstream_node`和`downstream_node`）建立与外部系统的接口，并通过`structures`字典管理内部设备。每个设备通过`ControlStructure`基类定义统一的流量计算接口`get_flow(H_up, H_down, setting)`，确保不同设备类型在系统中的一致性。系统通过`get_equations`方法构建非线性方程组，包括设备特性方程和可选的总体水量平衡方程，为数值求解提供基础。

```mermaid
classDiagram
class ComplexStationModel {
+str upstream_node
+str downstream_node
+Dict[str, ControlStructure] structures
+Dict[str, float] structure_settings
+bool enable_mass_balance
+get_variable_names() List[str]
+get_equations(variables, dt, prev_states) Dict[str, float]
+_calculate_mass_balance(variables) float
+add_structure(name, structure, initial_setting)
+remove_structure(name)
+set_structure_setting(name, setting)
+get_structure_setting(name) float
+calculate_total_capacity(H_up, H_down) float
}
class ControlStructure {
+str name
+get_flow(H_up, H_down, setting) float
}
class ConfigurableGateModel {
+float width
+float bottom_elevation
+float discharge_coefficient
+float submerged_threshold
+float current_opening
+get_equations(variables, dt, prev_states) Dict[str, float]
+calculate_discharge(H_upstream, H_downstream, opening) float
+_determine_flow_regime(H_up, H_down, opening) str
+_calculate_free_flow(H_up, opening) float
+_calculate_submerged_flow(H_up, H_down, opening) float
}
class ReservoirModel {
+Callable V_to_H_func
+Callable H_to_V_func
+float initial_volume
+float min_volume
+float max_volume
+get_equations(variables, dt, prev_states) Dict[str, float]
+_calculate_flow_balance(variables) tuple[float, float]
+_validate_physical_constraints(V, H) bool
}
class BoundaryCondition {
+str name
+BoundaryConditionConfig config
+get_equation(variables, time) Tuple[str, float]
+get_required_variables() List[str]
+validate_variables(variables) bool
}
class FlowBoundary {
+str node_name
+str flow_variable
+Callable[[float], float] flow_func
}
class StageBoundary {
+str node_name
+str stage_variable
+Callable[[float], float] stage_func
}
class RatingCurveBoundary {
+str flow_node
+str stage_node
+str flow_variable
+str stage_variable
+str curve_type
+Dict[str, Any] curve_params
+Callable[[float], float] curve_func
}
class BoundaryConditionManager {
+Dict[str, BoundaryCondition] boundary_conditions
+get_all_equations(variables, time) Dict[str, float]
+get_all_required_variables() List[str]
+validate_all_variables(variables) bool
}
ComplexStationModel --> ControlStructure : "包含"
ComplexStationModel --> ConfigurableGateModel : "使用"
ComplexStationModel --> ReservoirModel : "耦合"
ComplexStationModel --> BoundaryConditionManager : "依赖"
ConfigurableGateModel --> ControlStructure : "继承"
ReservoirModel --> ControlStructure : "继承"
BoundaryConditionManager --> BoundaryCondition : "管理"
BoundaryCondition <|-- FlowBoundary : "实现"
BoundaryCondition <|-- StageBoundary : "实现"
BoundaryCondition <|-- RatingCurveBoundary : "实现"
```

**图源**  
- [complex_station_model.py](file://waternet/models/complex_station_model.py#L43-L447)
- [configurable_gate_model.py](file://waternet/models/configurable_gate_model.py#L21-L547)
- [reservoir_model.py](file://waternet/models/reservoir_model.py#L26-L304)
- [boundary_conditions.py](file://waternet/models/boundary_conditions.py#L40-L138)

**节源**  
- [complex_station_model.py](file://waternet/models/complex_station_model.py#L43-L447)

## 设备层建模

设备层建模是复杂站模型的核心，通过`ConfigurableGateModel`类实现闸门动力学的精确描述。该模型基于配置文件驱动，支持自由出流、淹没出流和过渡流态的自动判别与计算。模型通过`calculate_discharge`方法实现流量计算，首先检查基本过流条件（开度、水位、水头差），然后计算淹没度（`H_down / H_up`）并根据预设阈值（`submerged_threshold`）判别流态。

对于自由出流，采用孔口流公式`Q = Cd * A * √(2g * H)`；对于淹没出流，采用淹没孔口流公式`Q = Cd * A * √(2g * (H_up - H_down))`，并引入淹没修正系数；对于过渡流态，则在自由出流和淹没出流流量之间进行线性插值。模型还支持堰流公式作为自由出流的替代计算方式，增强了对不同闸门类型的适应性。

```mermaid
flowchart TD
Start([开始计算闸门流量]) --> CheckConditions["检查过流条件<br/>开度>0, H_up>底高程, H_up≥H_down"]
CheckConditions --> |不满足| ReturnZero["返回流量=0"]
CheckConditions --> |满足| CalculateHeads["计算相对水头<br/>H_up_rel = H_up - 底高程<br/>H_down_rel = H_down - 底高程"]
CalculateHeads --> DetermineRegime["判别流态<br/>淹没度 = H_down_rel / H_up_rel"]
DetermineRegime --> FreeFlow{"淹没度 < 0.9*阈值?"}
FreeFlow --> |是| CalculateFree["计算自由出流流量<br/>Q = Cd * b * a * √(2gH)"]
DetermineRegime --> SubmergedFlow{"淹没度 > 1.1*阈值?"}
SubmergedFlow --> |是| CalculateSubmerged["计算淹没出流流量<br/>Q = 修正系数 * Cd * b * a * √(2gΔH)"]
DetermineRegime --> TransitionalFlow["计算过渡流态流量<br/>加权平均自由出流和淹没出流"]
CalculateFree --> RecordHistory["记录流态和流量历史"]
CalculateSubmerged --> RecordHistory
TransitionalFlow --> RecordHistory
RecordHistory --> ReturnFlow["返回计算流量"]
ReturnZero --> ReturnFlow
ReturnFlow --> End([结束])
```

**图源**  
- [configurable_gate_model.py](file://waternet/models/configurable_gate_model.py#L157-L204)

**节源**  
- [configurable_gate_model.py](file://waternet/models/configurable_gate_model.py#L21-L547)

## 控制策略集成

复杂站模型通过`structure_settings`字典集成设备控制策略，实现运行参数的动态调整。每个设备的控制参数（如闸门开度、泵站转速）通过`set_structure_setting`和`get_structure_setting`方法进行设置和获取。模型支持多种控制策略，包括恒定设置、水位控制、流量控制和协调控制。

在`reservoir_gate_channel_system.yaml`配置文件中，通过`control_strategy`字段定义控制模式（如`coordinated`协调控制）和流量分配比例。模型通过`optimize_settings`方法实现目标流量的优化调节，采用简化的比例调节算法，根据当前总流量与目标流量的比值调整所有设备的设置值，并限制调节幅度以保证稳定性。此外，边界条件框架通过`BoundaryConditionManager`统一管理流量边界、水位边界和水位流量关系边界，支持时变控制信号的集成。

```mermaid
sequenceDiagram
participant 控制器 as 控制器
participant 复杂站模型 as ComplexStationModel
participant 闸门模型 as ConfigurableGateModel
participant 边界管理器 as BoundaryConditionManager
控制器->>复杂站模型 : set_structure_setting(闸门名, 开度)
复杂站模型->>复杂站模型 : 更新structure_settings
控制器->>边界管理器 : add_boundary_condition(FlowBoundary)
边界管理器->>边界管理器 : 存储边界条件
控制器->>复杂站模型 : optimize_settings(目标流量, 上游水位, 下游水位)
复杂站模型->>复杂站模型 : 计算当前总流量
复杂站模型->>复杂站模型 : 计算调节比例因子
复杂站模型->>复杂站模型 : 按比例调整所有设备设置
复杂站模型->>复杂站模型 : 限制设置范围
复杂站模型-->>控制器 : 返回优化后的设置
控制器->>复杂站模型 : get_equations(变量, 时间步长, 前一时步状态)
复杂站模型->>闸门模型 : calculate_discharge(上游水位, 下游水位, 开度)
闸门模型-->>复杂站模型 : 返回计算流量
复杂站模型->>边界管理器 : get_all_equations(变量, 时间)
边界管理器-->>复杂站模型 : 返回边界条件方程
复杂站模型-->>控制器 : 返回完整方程残差
```

**图源**  
- [complex_station_model.py](file://waternet/models/complex_station_model.py#L43-L447)
- [configurable_gate_model.py](file://waternet/models/configurable_gate_model.py#L21-L547)
- [boundary_conditions.py](file://waternet/models/boundary_conditions.py#L40-L138)

**节源**  
- [complex_station_model.py](file://waternet/models/complex_station_model.py#L43-L447)

## 水力模型耦合方式

复杂站模型通过变量耦合和方程集成的方式与水力模型实现无缝耦合。在`get_equations`方法中，模型通过`variables`字典获取上下游水位（`H_up`和`H_down`），并为每个设备生成特性方程`Q_设备 - get_flow(H_up, H_down, setting) = 0`。这些方程与系统中其他模型（如明渠、管道）的方程共同构成非线性方程组，由求解器统一求解。

水库模型通过`ReservoirModel`类实现，其核心是水量平衡方程`V_current = V_prev + (Q_in - Q_out) * dt`和库容曲线约束`H_reservoir = V_to_H_func(V_current)`。水库的流入量和流出量通过`_calculate_flow_balance`方法从连接对象的流量变量中识别和累加，实现了与复杂站模型的流量耦合。边界条件通过`BoundaryConditionManager`管理，其生成的方程（如`Q_node - flow_value(t) = 0`或`H_node - stage_value(t) = 0`）直接加入系统方程组，确保边界条件的精确满足。

```mermaid
graph TD
subgraph "复杂站模型"
CS[ComplexStationModel]
CS --> |Q_设备| EQ1["设备特性方程<br/>Q_设备 - get_flow(H_up, H_down, setting) = 0"]
CS --> |enable_mass_balance| EQ2["总体水量平衡方程<br/>sum(Q_in) - sum(Q_out) = 0"]
end
subgraph "水库模型"
RM[ReservoirModel]
RM --> |V_current| EQ3["水量平衡方程<br/>V_current - V_prev - (Q_in - Q_out) * dt = 0"]
RM --> |H_reservoir| EQ4["库容曲线约束<br/>H_reservoir - V_to_H_func(V_current) = 0"]
end
subgraph "边界条件"
BC[BoundaryConditionManager]
BC --> |Q_node| EQ5["流量边界方程<br/>Q_node - flow_value(t) = 0"]
BC --> |H_node| EQ6["水位边界方程<br/>H_node - stage_value(t) = 0"]
BC --> |Q_node, H_node| EQ7["水位流量关系方程<br/>Q_node - f(H_node) = 0"]
end
subgraph "求解器"
Solver[非线性求解器]
Solver --> |求解| SystemEquations["系统方程组"]
end
EQ1 --> SystemEquations
EQ2 --> SystemEquations
EQ3 --> SystemEquations
EQ4 --> SystemEquations
EQ5 --> SystemEquations
EQ6 --> SystemEquations
EQ7 --> SystemEquations
SystemEquations --> Solver
```

**图源**  
- [complex_station_model.py](file://waternet/models/complex_station_model.py#L135-L183)
- [reservoir_model.py](file://waternet/models/reservoir_model.py#L103-L151)
- [boundary_conditions.py](file://waternet/models/boundary_conditions.py#L78-L98)

**节源**  
- [reservoir_model.py](file://waternet/models/reservoir_model.py#L26-L304)

## 梯级水库调度应用案例

梯级水库调度是复杂站模型的重要应用，通过`complex_hub_network.yaml`配置文件中的多级水库和枢纽站系统实现。该案例包含上游水库、调节水库、主控制枢纽、分水枢纽和输水管道等组件，形成一个复杂的梯级调度网络。上游水库作为水源，通过主控制枢纽的多孔闸门向输水主管和泄洪渠分流，输水主管经分水枢纽后与支流管道汇流，最终注入调节水库。

在调度过程中，主控制枢纽的闸门根据上游水库水位进行水位控制，确保上游水位稳定在目标值；分水枢纽的提升泵站根据需求流量进行流量控制，在特定时间启动。系统通过`config_driven_reservoir_gate_system.py`脚本实现配置驱动的仿真，支持恒定流测试和非恒定流测试（如阶跃响应）。仿真结果可用于评估调度方案的响应时间、超调量、调节时间和稳态误差等性能指标，为优化调度策略提供依据。

```mermaid
graph TB
subgraph "上游系统"
上游水库[(上游水库)]
上游水库 --> 主控制枢纽
主控制枢纽[主控制枢纽<br/>多孔闸门]
主控制枢纽 --> 主分流点
end
subgraph "分流系统"
主分流点[主分流点<br/>分流比 7:3]
主分流点 --> 输水主管
主分流点 --> 泄洪渠
输水主管[输水主管<br/>3.5km]
泄洪渠[泄洪渠<br/>1.2km]
end
subgraph "下游系统"
输水主管 --> 分水枢纽
分水枢纽[分水枢纽<br/>分水闸+提升泵]
分水枢纽 --> 输水主管_末端
支流管道[支流管道<br/>0.8km]
支流管道 --> 汇流点
输水主管_末端 --> 汇流点
汇流点[汇流点]
汇流点 --> 调节水库
调节水库[(调节水库)]
end
style 上游水库 fill:#f9f,stroke:#333
style 调节水库 fill:#f9f,stroke:#333
style 主控制枢纽 fill:#bbf,stroke:#333
style 分水枢纽 fill:#bbf,stroke:#333
style 主分流点 fill:#f96,stroke:#333
style 汇流点 fill:#f96,stroke:#333
style 输水主管 fill:#9f9,stroke:#333
style 泄洪渠 fill:#9f9,stroke:#333
style 支流管道 fill:#9f9,stroke:#333
```

**图源**  
- [complex_hub_network.yaml](file://examples/configs/complex_hub_network.yaml#L1-L228)
- [config_driven_reservoir_gate_system.py](file://examples/config_driven_reservoir_gate_system.py#L1-L430)

**节源**  
- [complex_hub_network.yaml](file://examples/configs/complex_hub_network.yaml#L1-L228)